#include <stdio.h>
#include <stdlib.h
#include <stdlib.h>

/*
 *KİŞİSEL VERİ ANALİZİ PROJESİ
 */

int main() {
    FILE * data
    data = fopen("data.txt", "r");
    if (data == NULL) {
        printf("Error in opening file\n");
        exit (EXIT_FAILURE);
    }


}